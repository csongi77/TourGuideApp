<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- List Item and Details text styles -->
    <!-- Title style -->
    <style name="entity_title_style">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/list_item_txt_title_textsize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- address style -->
    <style name="entity_address_style">
        <item name="android:paddingTop">@dimen/list_item_txt_address_padding_top</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/list_item_txt_address_textsize</item>
    </style>

    <!-- description style -->
    <style name="entity_description_style">
        <item name="android:paddingTop">@dimen/list_item_txt_address_padding_top</item>
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:textSize">@dimen/list_item_txt_address_textsize</item>
    </style>
</resources>
